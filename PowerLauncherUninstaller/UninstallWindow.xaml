<Window x:Class="PowerLauncherUninstaller.UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uninstall PowerLauncher" Height="320" Width="480"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1e1e2e" Foreground="White">
    <Grid Margin="32">
        <StackPanel x:Name="ConfirmPanel" VerticalAlignment="Center">
            <TextBlock Text="Uninstall PowerLauncher" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White"/>
            <TextBlock Text="This will remove PowerLauncher and all its files from your computer." FontSize="12" HorizontalAlignment="Center" Foreground="#aaa" Margin="0,8,0,4" TextWrapping="Wrap" TextAlignment="Center"/>
            <CheckBox x:Name="RemoveData" Content="Also remove game data and settings" Foreground="White" Margin="0,16,0,0" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                <Button Content="Uninstall" Click="Uninstall_Click" Padding="20,8" Background="#e53935" Foreground="White" BorderBrush="#c62828" Margin="0,0,8,0"/>
                <Button Content="Cancel" Click="Cancel_Click" Padding="20,8" Background="#3a3a5e" Foreground="White" BorderBrush="#555"/>
            </StackPanel>
        </StackPanel>
        <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" VerticalAlignment="Center">
            <TextBlock x:Name="StatusText" Text="Removing files..." FontSize="14" HorizontalAlignment="Center" Foreground="White"/>
            <ProgressBar x:Name="ProgressBar" Height="8" IsIndeterminate="True" Margin="0,16,0,0"/>
        </StackPanel>
        <StackPanel x:Name="DonePanel" Visibility="Collapsed" VerticalAlignment="Center">
            <TextBlock Text="PowerLauncher has been uninstalled." FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="White"/>
            <Button Content="Close" Click="Close_Click" Padding="20,8" Background="#3a3a5e" Foreground="White" BorderBrush="#555" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
