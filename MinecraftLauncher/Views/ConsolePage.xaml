<Page x:Class="MinecraftLauncher.Views.ConsolePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Console">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📋 Game Console" 
                       FontSize="28" FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="View game output and logs" 
                       FontSize="14" Opacity="0.7"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <ui:Button Content="Clear" Icon="{ui:SymbolIcon Delete24}" 
                       Click="Clear_Click" Margin="0,0,8,0"/>
            <ui:Button Content="Copy All" Icon="{ui:SymbolIcon Copy24}" 
                       Click="CopyAll_Click"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#1a1a2e" CornerRadius="8" Padding="12">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogOutput" 
                           FontFamily="Cascadia Code,Consolas,Courier New"
                           FontSize="12" Foreground="#00ff88"
                           TextWrapping="Wrap"
                           Text="[PowerLauncher] Ready. Launch a game to see output here."/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
