<Page x:Class="MinecraftLauncher.Views.ServersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Servers">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Servers" FontSize="28" FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="Manage favorite servers for quick connect" 
                       FontSize="14" Opacity="0.7"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:TextBox x:Name="ServerInput" PlaceholderText="Server address (e.g. play.example.com:25565)"/>
            <ui:TextBox x:Name="ServerName" Grid.Column="1" PlaceholderText="Name" 
                        Width="120" Margin="8,0,0,0"/>
            <ui:Button Grid.Column="2" Content="Add" Appearance="Primary" 
                       Click="Add_Click" Margin="8,0,0,0"/>
        </Grid>

        <ui:ListView Grid.Row="2" x:Name="ServerList" SelectionMode="Single">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="8,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="{Binding Address}" FontSize="11" Opacity="0.6"/>
                        </StackPanel>
                        <ui:Button Grid.Column="1" Content="Copy" Appearance="Secondary"
                                   Click="Copy_Click" Tag="{Binding}" Margin="4,0"/>
                        <ui:Button Grid.Column="2" Content="Remove" Appearance="Danger"
                                   Click="Remove_Click" Tag="{Binding}" Margin="4,0"/>
                    </Grid>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</Page>
