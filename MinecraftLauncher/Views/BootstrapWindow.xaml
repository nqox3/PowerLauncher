<ui:FluentWindow x:Class="MinecraftLauncher.Views.BootstrapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="PowerLauncher - Setup" 
        Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="PowerLauncher Setup" ShowMaximize="False"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="40">
            <ui:SymbolIcon Symbol="Rocket24" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <TextBlock x:Name="TitleText" Text="Setting up PowerLauncher..." 
                       FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock x:Name="StatusText" Text="Checking environment..." 
                       FontSize="13" HorizontalAlignment="Center" Margin="0,8,0,24"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <ProgressBar x:Name="ProgressBar" IsIndeterminate="True" Width="350" Height="6"/>
            <TextBlock x:Name="ProgressText" Text="" 
                       FontSize="11" HorizontalAlignment="Center" Margin="0,8,0,0"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
